# v0.15.19 検証記録

## 変更内容

- 手順表示の先頭に「⓪ 基本設定」を追加した。
- ⓪基本設定を「読込方法」「患者・施設情報」「設定」の3列へ整理した。
- 「前回記録を引き継ぐ」と「旧ODT読込」を読込方法欄へ移した。
- 対象者名が空の間は⓪基本設定を強調し、①保管は無色の待機表示にした。
- 対象者名が入力されると⓪を完了表示にし、①保管を青色の入力表示へ切り替える。
- 旧ODT読込ボタンの表示設定は移動後も維持される。

## 自動検証

```powershell
& '.\bill.venv311\Scripts\python.exe' -m unittest discover -s tests -p 'test_medi_sup*.py'
```

124件成功。

## 追加した画面検証

- 対象者名が空の場合、⓪のみが現在工程となり①が待機色になる。
- 対象者名の入力後、⓪が完了し①保管が入力色になる。
- 旧ODT読込が⓪基本設定内の読込方法欄に配置される。
